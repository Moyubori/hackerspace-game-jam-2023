name: hackerspace_game_jam_2023
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: 3.3.10

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  flame: ^1.6.0
  flame_forge2d: ^0.12.5
  flame_tiled: ^1.9.1
  flame_audio: ^1.4.0
  flame_isolate: ^0.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/audio/